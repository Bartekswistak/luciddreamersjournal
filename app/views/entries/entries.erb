<h1><%= current_user.username + "'s Dreams" %></h1>

<% @entries.each do |entry| %>
  <div>
    <%= "  #{entry.date}" %>
    <%= "~  #{entry.content}" %>
    <a href="/entries/<%=entry.id%>/edit">Edit Entry </a>
  </div>
<%end%>
